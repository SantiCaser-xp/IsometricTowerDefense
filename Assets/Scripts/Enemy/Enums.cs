    public enum TargetType
    {
        Tower,
        PlayerBase,
        Barricade
    }
    public enum TargetingStrategy
    {
        Nearest,
        Weakest,
        Strongest,
        PlayerBase,
        Random
    }

